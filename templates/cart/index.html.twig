{% extends 'base.html.twig' %}

{% block title %}{% trans %}Cart{% endtrans %}{% endblock %}

{% block body %}
    <div class="box text-center">
        <div class="row">
            <div class="col-md-10 offset-md-1">
                <h1>Shopping cart</h1>
                <p class="text-muted">You currently have 3 item(s) in your cart.</p>
            </div>
        </div>
    </div>
<div class="row">
    <div id="basket" class="col-lg-9">
        <div class="box">
            <form method="get" action="checkout1.html">
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                        <tr>
                            <th colspan="2">Product</th>
                            <th>Quantity</th>
                            <th>Unit price</th>
                            <th>Discount</th>
                            <th colspan="2">Total</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% set total = 0 %}
                        {% for c in cart %}
                            {% set total = total +  c.price*c.amount %}
                            <tr>
                                <td><a href="{{ path('product_show', {'id' : c.pid }) }}"><img src="https://d19m59y37dris4.cloudfront.net/minimal/2-0/img/detailsquare.jpg" alt="White Blouse Armani"></a></td>
                                <td><a href="{{ path('product_show', {'id' : c.pid }) }}">{{ c.title}}</a></td>
                                <td>
                                    <input value="{{ c.amount }}" class="form-control" type="number">
                                </td>
                                <td>{{ c.price }}</td>
                                <td>$0.00</td>
                                <td>{{ c.price*c.amount }}</td>
                                <td><a href="{{ path('cart_remove', {'id' : c.id }) }}"><i class="fa fa-trash-o"></i></a></td>
                            </tr>
                        {% endfor  %}
                        <tfoot>
                        <tr>
                            <th colspan="5">Total</th>
                            <th colspan="2">{{ total }} &#8381;</th>
                        </tr>
                        </tfoot>
                    </table>
                </div>
                <div class="box-footer">
                    <div class="pull-left"><a href="{{ path('main') }}" class="btn btn-outline-white-secondary"><i class="fa fa-chevron-left"></i> Continue shopping</a></div>
                    <div class="pull-right">
                        {% if total %}
                            <button class="btn btn-outline-white-secondary"><i class="fa fa-refresh"></i> Update cart</button>
                            <button type="submit" class="btn btn-outline-white-primary">Proceed to checkout<i class="fa fa-chevron-right"></i></button>
                        {% endif %}
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
{% endblock %}
