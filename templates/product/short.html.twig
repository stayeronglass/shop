<div class="col-md-4 col-sm-6">
    <div class="product">
        <div class="image">
            <a href="{{ path('product_show', {'id' : product.id }) }}">
                <img src="/upload/{{ product.image_name[:1] }}/{{ product.image_name[1:1] }}/{{ product.image_name}}.{{ product.image_ext}}" alt="" class="img-fluid image1" />
            </a>
        </div>
        <!-- /.image-->
        <div class="text">
            <h3><a href="{{ path('product_show', {'id' : product.id }) }}">{{  product.title }}</a></h3>
            <p class="price">

                <del>$280</del>&nbsp;{{  product.price }} &#8381; &nbsp;&nbsp;
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a href="javascript:void(0)" onclick="add(this, {{ product.id }})" class="btn btn-primary mr-1" title="в корзину"><i class="fa fa-shopping-cart"></i></a>
                {% else %}
                    <button type="button" data-toggle="modal" data-target="#login-modal" class="btn btn-outline-secondary mr-1">
                        <i class="fa fa-shopping-cart"></i>
                    </button>
                {% endif %}
            </p>

        </div>
        <!-- /.text-->
    </div>
    <!-- /.product-->
</div>
