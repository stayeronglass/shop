{% extends 'base.html.twig' %}

{% block title %}Hello {% endblock %}

{% block body %}
    <div class="box text-center">
        <div class="row">
            <div class="col-md-10 offset-md-1">
                <h1 class="capitalize_first_letter">{% trans %}my orders{% endtrans %}</h1>
                <p class="lead capitalize_first_letter">{% trans %}orders information{% endtrans %}.</p>
            </div>
        </div>
    </div>
    <div class="row">
        <!-- *** LEFT COLUMN ***-->
        <div id="customer-account" class="col-lg-9">
            {% if orders %}
            {% else %}
                <p>you have no orders!</p>
            {% endif %}
        </div>
        <!-- *** LEFT COLUMN END ***-->

        <!--  *** RIGHT COLUMN ***-->
        {{ render(controller('App\\Controller\\LkController::right', { 'active': 'orders'} )) }}
        <!--  *** RIGHT COLUMN END ***-->
    </div>

{% endblock %}

