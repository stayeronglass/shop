{% extends 'base.html.twig' %}

{% set title = 'Категории' ~ ' | ' ~ title_postfix %}
{% block body %}
    <!-- *** ADVANTAGES ***-->
    <div id="advantages">
        <div class="box text-center">
            <h3 class="text-uppercase">Категории</h3>
            <p>222</p>
            <div class="same-height-row row">
                {% for category in categories %}
                    {% include 'category/item.html.twig' with {'category': category} %}
                {% endfor %}
            </div>
        </div>
    </div>
    <!-- /#advantages-->
    <!-- *** ADVANTAGES END ***-->
{% endblock %}
