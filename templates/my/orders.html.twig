{% extends 'base.html.twig' %}

{% block title %}Hello {% endblock %}

{% block body %}
    <div class="box text-center">
        <div class="row">
            <div class="col-md-10 offset-md-1">
                <h1 class="capitalize_first_letter">{% trans %}my orders{% endtrans %}</h1>
                <p class="lead capitalize_first_letter">{% trans %}orders information{% endtrans %}.</p>
            </div>
        </div>
    </div>
    <div class="row">
        <!-- *** LEFT COLUMN ***-->
        <div id="customer-orders" class="col-lg-9">
            <div class="box">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                        <tr>
                            <th>{% trans %}№{% endtrans %}</th>
                            <th class="capitalize_first_letter" >{% trans %}order{% endtrans %}</th>
                            <th class="capitalize_first_letter">{% trans %}action{% endtrans %}</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for order in orders %}
                            <tr>
                                <td># {{ order.id }} </td>
                                <td>{{ order.address }} </td>
                                <td><a href="{{ path('my_order_show', {'id':order.id }) }} " class="btn btn-outline-primary btn-sm">{% trans %}show{% endtrans %}</a></td>
                            </tr>
                        {% else %}
                            <tr>
                                <td class="font-weight-bold">{% trans %}no orders found{% endtrans %}</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
                <!-- /.table-responsive-->
            </div>
            <!-- /.box-->
        </div>
        <!-- *** LEFT COLUMN END ***-->

        <!--  *** RIGHT COLUMN ***-->
        {{ render(controller('App\\Controller\\MyController::right', { 'active': 'orders'} )) }}
        <!--  *** RIGHT COLUMN END ***-->
    </div>
{% endblock %}

